{
  "name": "codex-ledger",
  "version": "0.1.0",
  "description": "CLI for ledgering AI-assisted code changes.",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "ledger": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}